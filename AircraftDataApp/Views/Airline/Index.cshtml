@model IEnumerable<AircraftDataApp.Models.Airline>

@{
    ViewData["Title"] = "Index";
}

<h1>Airlines</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AirlineName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Country)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AirlineName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Country)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAirline">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAirline">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
